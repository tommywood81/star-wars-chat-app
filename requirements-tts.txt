# Minimal TTS requirements - only essential packages
fastapi==0.95.2
uvicorn==0.22.0
pydantic==1.10.7
python-multipart==0.0.6
requests==2.31.0

# Note: TTS will be installed separately if needed
# For now, we'll use a mock TTS service for faster builds
